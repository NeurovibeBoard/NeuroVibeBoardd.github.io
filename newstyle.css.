body {
  font-family: Arial, sans-serif;
  background: #0f1218;
  color: #fff;
  margin: 0;
  padding: 40px;
}

h2 {
  text-align: center;
  animation: fadeInDown 1.2s ease forwards;
}

.contact-container {
  max-width: 600px;
  margin: 40px auto;
  background: #1a1f2e;
  padding: 30px;
  border-radius: 12px;
  box-shadow: 0 8px 24px rgba(0,0,0,0.4);
  opacity: 0;
  transform: translateY(40px);
  animation: slideUp 1s ease forwards;
}

form {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

label {
  font-weight: bold;
}

input, textarea {
  padding: 12px;
  border-radius: 8px;
  border: none;
  outline: none;
  font-size: 14px;
  transition: box-shadow 0.3s ease;
}

input:focus, textarea:focus {
  box-shadow: 0 0 8px #5ac8fa;
}

textarea {
  resize: vertical;
  min-height: 120px;
}

.btn {
  padding: 12px;
  background: linear-gradient(135deg, #5ac8fa, #7c5cff);
  border: none;
  border-radius: 8px;
  font-weight: bold;
  color: #0f1218;
  cursor: pointer;
  transition: transform 0.2s ease;
  animation: pulse 2s infinite;
}

.btn:hover {
  transform: scale(1.05);
}

/* Phone numbers styling */
.phone-list {
  margin-top: 20px;
  list-style: none;
  padding: 0;
}
.phone-list li {
  margin: 8px 0;
}
.phone-list a {
  color: #5ac8fa;
  text-decoration: none;
}
.phone-list a:hover {
  text-decoration: underline;
}

/* Animations */
@keyframes fadeInDown {
  from { opacity: 0; transform: translateY(-20px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes slideUp {
  from { opacity: 0; transform: translateY(40px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes pulse {
  0% { box-shadow: 0 0 0 0 rgba(92, 200, 250, 0.7); }
  70% { box-shadow: 0 0 0 20px rgba(92, 200, 250, 0); }
  100% { box-shadow: 0 0 0 0 rgba(92, 200, 250, 0); }
}
